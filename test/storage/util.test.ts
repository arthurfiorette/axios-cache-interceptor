import type { AxiosStorage } from '../../src/storage/storage';

describe('tests common storages', () => {
  it('', () => {
    //TODO: Add storage tests
    const a: AxiosStorage | false = false;

    expect(a).toBe(false);
  });
});
